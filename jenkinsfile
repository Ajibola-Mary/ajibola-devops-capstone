pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build Java') {
            steps {
                sh 'mvn -f java/pom.xml clean package'
            }
        }

    }
}
